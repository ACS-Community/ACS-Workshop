<SimulatedComponent xmlns="urn:schemas-cosylab-com:SimulatedComponent:1.0"
                    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <_corbaMethod Name="observe"
           Timeout="0">
import os
from time import sleep
file = open(os.environ['INTROOT'] + '/image.jpg')
str = file.read()
file.close()
sleep(2)
return bytes(str)
  </_corbaMethod>
</SimulatedComponent>
